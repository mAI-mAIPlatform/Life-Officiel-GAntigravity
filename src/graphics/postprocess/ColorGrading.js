import * as THREE from 'three';

export class ColorGrading {
    constructor(composer) {
        // Post-processing LUT color grading placeholder
        // Requires passes from 'three/examples/jsm/postprocessing/ShaderPass.js'
        this.composer = composer;
    }
}
